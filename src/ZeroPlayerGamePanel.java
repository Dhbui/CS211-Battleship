import javax.swing.*;

/**
 * The beginnings of a structure for the zero player GUI functionality. Zero player is watching two cpu's play against
 * each other.
 * @author Dylan Bui
 */
public class ZeroPlayerGamePanel extends  JPanel {
    private JFrame myFrame;

    public ZeroPlayerGamePanel(JFrame f) {
        myFrame = f;
    }
}
